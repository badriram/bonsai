# Built binary at the repo root. Anchored with leading / so this does NOT
# match cmd/bonsai/ — which is the CLI's main package and must be tracked.
/bonsai
/dist/
/bin/
*.test
*.out
.env
.env.*
!.env.example
.DS_Store
.idea/
.vscode/
.claude/
